# Override ESP32 settings for simulator - build for host
[build]
# No target specified = build for host

# Override parent's rustflags that include -nostartfiles (which breaks host builds)
[target.x86_64-unknown-linux-gnu]
rustflags = []
